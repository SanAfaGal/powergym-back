# =============================================================================
# Docker Ignore File
# =============================================================================
# Files and directories to exclude from Docker build context
# =============================================================================

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
dist/
build/
*.egg
.venv/
venv/
env/
ENV/

# Environment files (use --env-file instead)
# Exclude all .env files from Docker build context
.env
.env.*
!.env.example
# Note: .env files are mounted at runtime, not included in image

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/
tests/
test_*.py
*_test.py

# Logs
*.log
logs/
*.tmp

# Database
*.db
*.sqlite
*.sqlite3

# Docker
# Exclude Docker files from build context (not needed in image)
Dockerfile*
docker-compose*.yml
.dockerignore
# Keep docker-compose.example.yml if needed for reference (but typically not needed in image)

# Git
.git/
.gitignore
.gitattributes

# Documentation
*.md
!README.md
docs/

# Scripts (not needed in production image)
scripts/
SETUP_GUIDE.md

# Keep Alembic for migrations (needed in production)
# alembic/versions/

# Development files
FRONTEND_REWARDS_IMPLEMENTATION.md

# OS files
Thumbs.db
.DS_Store

# Temporary files
*.bak
*.swp
*.tmp

# Large model files (if models are loaded from external storage)
# InsightFace downloads models automatically - they're not needed in image
# Models are stored in persistent volume, not in image
*.onnx
*.pth
*.h5
*.pb
weights/
*.weights
# InsightFace models directory (downloaded at runtime to volume)
.insightface/
~/.insightface/

# Large data files
*.csv
*.json
*.xml
data/
datasets/

# Node modules (if any)
node_modules/

# Coverage reports
.coverage
htmlcov/

# Jupyter notebooks
*.ipynb
.ipynb_checkpoints/

# Deployment scripts (not needed in image)
deploy.sh
rebuild.sh

# Uploads directory (mounted as volume, not needed in image)
uploads/
uploads/*

# Local development files
*.local
*.local.*

